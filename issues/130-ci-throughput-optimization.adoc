= CI実行時間の最適化
:status: completed
:depends-on: 132

== 目的

品質、current stable Rust、release candidateおよびpublishの検証責務を維持したまま、GitHub Actionsの待ち時間、
重複buildおよび不要なartifact転送を減らす。

== 作業

* workflow jobごとのwall-clock time、queue time、cache hit率およびartifact sizeを計測する。
* quality、native target build、WASM、Zedおよびrelease artifactの依存graphを見直す。
* Rust、NixおよびNode cacheのkey・復元範囲を、lockfileとtargetに基づき再設計する。
* security、current stable Rust、cross-runtime、release contractおよび公開前の完全性検証を省略しないことをpolicy testで固定する。

== 完了条件

* 現在の必須検証を保持した比較可能な計測結果と、採用した最適化の根拠を記録する。
* fork、pull request、main、tagの権限境界とartifact provenanceを弱めない。

== 計測基準

測定対象はGitHub Actionsのjob APIが返す`created_at`、`started_at`および`completed_at`とする。queue timeは
`started_at - created_at`、wall-clock timeは`completed_at - started_at`で定義する。cache hit率は、明示的な
cache actionが存在せずNix binary cacheのsubstitute結果もActions APIから取得できないため、このCIの最適化指標には
含めない。代わりにlockfile固定とjobの依存edgeを再現可能な最適化単位とする。

[cols="2,2,2,3",options="header"]
|===
|基準run |対象 |wall-clock time |観測

|https://github.com/KeishiS/AdocWeave/actions/runs/30002513033[v0.2.0 tag run]
|全workflow
|15分34秒
|2026-07-23。旧graphではquality完了（8分35秒）の後にcandidate buildを開始していた。

|同run
|`quality / verify`
|8分35秒
|最長quality job。MSRVは1分未満であり、current stable方針への統合によって独立jobを廃止した。

|同run
|candidate build（最長native）
|3分32秒
|旧graphではqualityの後に開始していた。新graphではplan完了後にsource qualityおよびfuzzと並列に開始する。

|https://github.com/KeishiS/AdocWeave/actions/runs/30005672994[最適化後main run]
|candidate受け入れ完了まで
|8分15秒
|2026-07-23。全必須jobが成功し、publishだけはnon-tagのためskipした。旧tag runの同じ境界は15分00秒であり、6分45秒（45%）短縮した。

|同run
|`quality / verify`
|3分52秒
|quality、fuzz、candidate buildが同時に開始した。source変更およびNix cache状態の影響を含むため、このjob単独の差分を並列化の効果とは扱わない。

|同run
|queue time
|最大3秒
|skip以外の全jobで`started_at - created_at`を測定した。runner待機が主経路を支配していないことを確認した。
|===

同runのartifact upload sizeはnative x86-64が1,462,400 bytes、native ARM64が1,235,166 bytes、globalが
219,773 bytes、集約candidateが2,933,999 bytesであった。最適化後runは順に1,461,804 bytes、1,234,850 bytes、
219,753 bytes、2,933,059 bytesであり、candidateのbyte列とprovenanceを保ったまま転送量を増やしていない。
中間artifactの保持期間は7日、集約candidateは14日を維持する。

== 採用した最適化

* source quality、fuzz、pull requestのNix package検証を独立jobに分離した。
* mainおよびtagではcandidate buildをplan直後に開始し、publishだけがquality、fuzz、candidate verificationおよび
  installation E2Eの全成功を待つ。
* mainおよびtagで重複していたx86-64のNix package buildを省き、candidate installation E2EのLinux 2 architectureで
  同じpackage検証を実施する。
* native release buildをNix development shellで実行し、runnerの`rustup`ではなく、release manifestと一致する
  lockfile固定Rust toolchainを使用する。

== 完了確認

最適化後main runではdependency governance、source quality、fuzz、Linux 2 architectureのnative build・smoke・
Nix package検証、candidate metadata verificationおよびinstallation E2Eがすべて成功した。pull requestではNix package
jobを独立して実行し、main／tagでは同じ検証をLinux 2 architectureのinstallation E2Eへ移すため、eventごとの
権限境界、artifact provenanceおよび公開前の検証責務は変わらない。workflow policy testはこの依存edge、Nix toolchain
および最小権限を検査する。

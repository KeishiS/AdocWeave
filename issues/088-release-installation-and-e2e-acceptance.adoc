= Release導入文書とend-to-end受け入れ
:status: completed
:depends-on: 086,087,109

== 目的

GitHub Releaseまたは公開Nix flakeから初めて導入する利用者とfrontend integratorが、手動Cargo buildなしで
asset／flake revisionを選択、検証、install、更新および削除できることを最終的に確認する。

== 作業

* OS／architectureから正しいCLI・LSP assetを選ぶ表と、checksum／attestation検証手順を追加する。
* user-local install先、system-wide install、PATH、shell completion、更新、version固定およびuninstallを
  platform別に記載する。`curl | sh`を唯一の手順にしない。
* browser libraryの静的hosting、ES module／WASM MIME type、CSP、cross-origin isolation、cache busting、
  integrity確認およびリアルタイムHTML previewの利用例をfrontend向け文書へ追加する。
* Zed Gallery導入、binary cache、PATH override、offline動作および完全削除を開発版手順と分離する。
* GitHub prereleaseをclean Linux x86-64、Linux ARM64、browser frontendおよびLinux版Zed profileへ導入する
  E2E checklistを実行する。macOS／Windowsは将来targetの受け入れIssueへ分離する。
* Issue 109のflakeをcommitへ固定し、Linux x86-64／ARM64で`nix run`、profile導入およびNixOS
  `environment.systemPackages`評価を確認する。
* release noteに対応target、契約version、breaking change、既知の制約、asset verificationおよびupgrade手順を
  必須化する。
* 配布元はリポジトリ所有者自身のGitHub Releasesだけとする。package manager、container registry、
  crates.io、npmおよびZed Registryへの公開は行わず、導入経路もGitHub Releasesだけで完結させる。
* private repositoryで検証する場合は、認証済みGitHub CLIまたは最小権限tokenを使う手順と、credentialを
  archive、logおよび設定例へ残さないことを確認する。

== 完了条件

source checkoutと手動Rust toolchainのないclean環境でCLI、LSP、browser library、Zed連携およびNix flake導入を確認し、
install前後のfilesystem差分からuninstall後に管理対象fileが残らないことを確認する。最終手順を
`docs/release-checklist.adoc`と利用者向け導入文書へ反映する。

== 実装状況

* xref:../docs/release-installation.adoc[利用者向け導入文書]へasset選択、checksum／attestation、user-local／
  system-wide配置、PATH、version固定、rollback、browser hosting、Zed、更新および削除手順を集約した。
* `release-installation-e2e.mjs`は隔離HOMEへCLI、LSP、browser libraryおよびZed拡張を導入し、clean PATH、
  version symlink、公開入口およびuninstall後のfile集合一致を検査する。
* Galleryへ公開しない方針とsource checkout不要の完了条件を両立するため、version付き
  `adocweave-zed-<version>.tar.xz`を6番目のarchiveとして配布契約へ追加した。
* 公開済み`v0.1.0-rc.1`を変更せず、新しいasset契約と公開契約version 2をrelease candidateへ分離した。
  失敗した`v0.1.0-rc.2` tagも付け替えず、Chromium終了競合の修正を含む`v0.1.0-rc.3`を受け入れ対象とする。
* `v0.1.0-rc.3`の公開と必須Actionsは完了した。その後Issue 107で公開APIをversion 3へ変更したため、
  現行成果物の最終受け入れ対象は`v0.1.0-rc.4`へ進める。RC.3のtagとReleaseは変更しない。
* `v0.1.0-rc.4`の公開後、Issue 108で失敗詳細の非漏洩境界を破壊的に修正したため、最終受け入れ対象を
  `v0.1.0-rc.5`へ進める。RC.4以前のtagとReleaseは変更しない。
* GitHub Release noteへ対応target、公開契約とbreaking change、既知制約、asset検証および
  upgrade／rollbackを必ず追加する生成・検証処理をpublish境界へ追加した。
* local x86-64成果物では、Zed archiveの決定性検査と導入・完全削除E2Eが成功した。
* Release workflowは集約candidateをx86-64／ARM64のnative runnerへ戻し、両architectureの導入・完全削除E2Eが
  成功するまでpublish jobへ進まない。
* pull requestは完全local quality gateとplan、`main`へのpushはさらに非公開candidate検証を起動し、tag作成前にmain
  commitの成功結果を確認できるようにした。通常のmain pushではpublish jobを起動しない。

== 残る受け入れ

`v0.1.0-rc.5`を公開した後、source checkoutとRust toolchainを持たないclean Linux x86-64／ARM64で実Release assetを
使うE2Eを実行する。CLIとLSPに加え、空labelのxrefへResolverの`displayText`を渡すnative／WASM契約を確認する。
browserは実配備header、Zedは初回managed LSP取得、offline再起動およびUIからの削除までを確認する。
Nixは公開flake URLをRC.5 commitへ固定し、`nix run`、profile install／removeおよびNixOS評価をLinux両architectureで
確認する。この公開と実機操作はrepository所有者のpush、tag作成、Environment承認および対応環境を必要とする。

== RC.5公開記録

2026-07-22に`v0.1.0-rc.5`をcommit
`ad268fb7135187680df870c1bd82add1f340e77d`からprereleaseとして公開した。GitHub Actions
https://github.com/KeishiS/AdocWeave/actions/runs/29932301678[run 29932301678]ではquality、依存監査、MSRV、
Linux x86-64／ARM64 native build・smoke、candidate検証、Nix package／NixOS評価を含む導入E2Eおよびpublishが
すべて成功した。

公開後に次を再検証した。

* 公開9 assetを新しい一時directoryへ取得し、`sha256.sum`と配布metadataがsource commitに一致した。
* 公開GitHub APIで9 assetすべてに同じrepository由来のArtifact Attestation bundleが存在することを確認した。
* `nix run github:KeishiS/AdocWeave/v0.1.0-rc.5 -- --version --json`がRC.5と契約versionを返した。
* 隔離したx86-64 Linux profileで公開flakeをinstallし、CLI／LSPのversionを確認してから完全にremoveできた。
* 2026-07-22に隔離したZed profileへRC.5公開拡張を導入し、managed LSP取得、editor機能、offline再起動および
  削除を確認した。再現手順はxref:../docs/release-installation.adoc[Release導入文書]へ記録した。

== 実機確認の代替判断

2026-07-22、repository所有者はARM64実機とbrowser実配備環境を利用できないため、次の自動証拠をもって当該項目を
受け入れると判断した。

* GitHub ActionsのARM64 native runnerで、archive起動、Nix package build、NixOS評価および導入・完全削除E2Eが成功した。
* browser archiveについて、native／WASM conformance、型検査、package構造、size、bundler、fallbackおよび
  Release archive内WASMの実行が必須gateで成功した。
* 実Chromiumと配備先固有のMIME、CSP、COOP／COEPはIssue 106の方針どおり任意gateであり、stable公開の必須条件としない。

これは未実施の実機確認を実施済みと偽るものではなく、利用可能な自動証拠に基づく明示的なrisk acceptanceである。
将来ARM64実機またはbrowser配備環境を利用できる時点で追加確認し、不一致があれば新しいIssueとして扱う。
RC.5の公開asset、checksum、metadata、attestation、Linux両architectureの自動E2E、公開Nix flake、x86-64 profile lifecycle
およびZed実機確認が完了したため、Issue 088を完了とする。

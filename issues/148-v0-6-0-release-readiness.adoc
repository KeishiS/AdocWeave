= v0.6.0 release readiness
:status: in-progress
:depends-on: 147

== 目的

公開文書属性occurrence queryを`v0.6.0`として配布する。版数、移行ガイド、テスト用入力、
リリース前の確認を一つのリリース作業として同期する。

== 設計判断

release manifest（`packageVersion` 0.6.0）をリリース版数の機械可読な基準とする。
workspace、Zed、browser、distribution planおよびテスト用入力の一致は`cargo make release-contract`で検証する。
ADR 0006に従い、mainの完全candidate成功commitへstable tagを一度だけ付ける。

== 完了条件

* workspace、Zed、browser、release manifest、distribution planおよびfixtureが`0.6.0`で一致する。
* `docs/upgrade-0-6.adoc`が公開occurrence queryへの移行を定義する。
* `cargo make release-gate`と`node tools/release-contract.mjs --tag=v0.6.0`が成功する。
* mainのcandidate成功後、`v0.6.0`を作成してGitHub Actionsと公開assetを受け入れる。

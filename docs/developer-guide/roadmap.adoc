= 今後の予定

この文書では、現在進行中の作業と着手を予定している作業の方向を示します。実施時期や公開日を
約束するものではありません。各作業の詳しい内容、依存関係および完了条件は、リンク先の
GitHub Issueを参照してください。

完了した作業の経緯はIssue、versionごとの変更は
https://github.com/KeishiS/adocweave/releases[GitHub Releases]のRelease Notesに記録します。
このページには完了したmilestoneの履歴を残しません。

== 公開Rust APIのversion管理

公開Rust APIの入口の整理は完了しました。解析は``Engine::analyze``と``analyze_with``、前処理は
``preprocess``系の4つの関数と``EffectiveProcessingOptions``のmethodに揃っています。次は、この形を
保ちながら版を重ねられるようにします。

* https://github.com/KeishiS/adocweave/issues/265[公開Rust APIのSemVer差分検査]

入口の整理を終えた版のtagを、SemVer差分検査の最初の比較基準とします。整理する前に基準を固定していれば、
直後のminor版で大量の差分を意図した破壊的変更として記録する作業が生じていました。

差分検査では、patch版へ意図しない破壊的変更を含めず、0.yのminor版で破壊的変更を行う場合は、
機械可読な差分記録とRelease Notesの移行手順を対応付けます。検査に使うtool、比較対象のtag、
対象crateおよびrelease種別を固定し、実行時に取得した最新版へ判定を依存させません。

== golden fixtureの保守

``fixtures/public-conformance/``には、現在の実装から成果物を書き出す再生成コマンドと、
ディスク上のファイルが再生成結果と一致することを検査するテストの対があります。共有fixtureである
``fixtures/conformance/``には、そのどちらもありません。

* https://github.com/KeishiS/adocweave/issues/320[共有conformance fixtureの再生成コマンドと冪等性検査]

そのため公開契約が増えるたびに複数のファイルを手作業で更新する必要があり、整形も3通りに分かれています。
更新漏れはテストが落ちるまで気付けません。

== Language Serverの応答性

読み取り専用の要求は専用のCPUプールで処理しますが、通知の処理はイベントループ上で同期実行します。
大きなワークスペースでは、初期化中と打鍵中にほかの要求へ応答できません。

* https://github.com/KeishiS/adocweave/issues/308[通知処理のイベントループからの退避]

== CLIとLanguage Serverの挙動差

同じ製品の中で、終了コードの規約、設定の適用範囲および設定読み込みの資源境界が分かれています。
文書の記述と実装が一致していない箇所もあります。

* https://github.com/KeishiS/adocweave/issues/309[CLIとLanguage Serverの挙動差の解消]

終了コードの変更は破壊的変更です。ほかの破壊的変更と同じrelease trainでまとめるか、
単独のminor版で行うかは着手時に判断します。

== 文書の用語統一

日本語の地の文に一般名詞の英語表記が残っています。「専門分野が異なる学部生でも前から順に読める
文章」という目標に対して、この混在が最大の障害です。

* https://github.com/KeishiS/adocweave/issues/310[日本語文書の用語統一]

一括置換はせず、用語表と判定基準を決めてからuser-guideより順に進めます。

== 保留中の対話的な確認環境

公開済みのBrowser APIとHTML契約を利用し、Webブラウザーだけで文書を試せる環境を提供する案を
保留しています。

* https://github.com/KeishiS/adocweave/issues/84[公式Playground]

公式Playgroundの公開には、配信先、公開URL、公開作業を行う権限および問題発生時に以前の状態へ戻す
権限を決める必要があります。これらが決まるまで実装済みのローカルプレビューとは分離し、
製品versionの更新やほかの機能を公開するための前提にはしません。

== 条件付き候補

次の作業は具体的な需要や運用上の問題が確認されるまで着手しません。

* https://github.com/KeishiS/adocweave/issues/33[単体CLIのlocal resource profile評価]
* https://github.com/KeishiS/adocweave/issues/34[RenovateによるNix依存更新の評価]

単体CLIのlocal resource profileは、相対resourceを解決する需要が確認された場合に再評価します。
RenovateによるNix依存更新は、手動更新が継続的な負担となった場合に再評価します。

== 実施しない公開経路

extension registry、public package registryおよびOSのpackage managerへの登録・公開は行いません。
次のIssueは実装候補ではなく、この判断と過去の検討内容を残す記録として保留します。

* https://github.com/KeishiS/adocweave/issues/82[公式extension registry]
* https://github.com/KeishiS/adocweave/issues/83[crates.io・npm公開の評価]
* https://github.com/KeishiS/adocweave/issues/86[Homebrew・WinGet・Scoopの評価]

= Core 0.1 置換モデル

置換は出力バックエンドの実行前に意味コンテキストから決定する。HTML固有の
再解析処理として実装してはならない。

[cols="2,2,1,1,2",options="header"]
|===
|コンテキスト |インライン解析 |属性展開 |Raw HTML |バックエンドのエスケープ

|段落と見出しのテキスト
|constrainedな等幅、強調、斜体、属性参照、リンク、xref
|単純な属性参照を一回展開
|禁止
|必須

|インライン等幅リテラル
|なし
|なし
|禁止
|必須

|リテラルブロック
|なし
|なし
|禁止
|必須

|ソースブロック本体
|なし
|なし
|禁止
|必須

|ソース言語
|なし
|なし
|禁止
|出力先のメタデータとして無害化

|`Unsupported`ノード
|なし
|なし
|禁止
|寛容モードでは必須

|リンクターゲット
|なし
|単純な属性参照を一回展開
|禁止
|URLポリシー検査後に属性値としてエスケープ

|リンク・xrefの表示文字列
|等幅、強調、斜体、属性参照、リンク、xref
|単純な属性参照を一回展開
|禁止
|必須

|xrefターゲット
|なし
|なし
|禁止
|解決済みhrefだけをURLポリシー検査後にエスケープ

|文書属性値
|なし
|なし。再帰展開しない
|禁止
|利用コンテキストに応じて必須

|数式
|未対応
|なし
|禁止
|未対応構文またはテキストとして扱う
|===

semantic ASTでは`Text`、`Literal`、`Styled`、`AttributeReference`、`Link`および
`Reference`がこの区別を表現する。

- `Text`は出力先バックエンドが解釈する通常テキストである。
- `Literal`は不透明な内容であり、再帰的な置換を行わない。
- `Styled`は再帰的に解析した意味上の子要素を所有する。
- `Link`はURLターゲットと意味解析済み表示文字列を分離する。
- `Reference`は参照先表記と意味解析済み表示文字列を分離し、解決をホストへ委譲する。

出力バックエンドはこのASTだけを入力とし、ソースマークアップを再解析してはならない。
HTMLはテキストを含む全コンテキストで`&`、`<`、`>`、`"`、`'`をエスケープする。
将来の非HTMLバックエンドも、解析動作を変更せず出力形式に対応する安全規則を適用する。

名前に`substitutions`を含むテストがコンテキスト横断の動作を固定する。

== 将来の置換コンテキストの追加

後続Issueでリストまたは数式等を追加する場合、構文ノードと同じ変更で
この表へ行を追加しなければならない。各行は少なくとも、インライン解析、属性展開、
raw出力の可否およびバックエンド側の安全化責任を定義する。

置換済みの意味構造はsemantic ASTへ格納する。HTMLその他のバックエンドが元ソースを
再解析したり、独自に属性展開したりしてはならない。不透明コンテキストは`Literal`または
対応するブロックノードとして保持し、子要素の置換を禁止する。

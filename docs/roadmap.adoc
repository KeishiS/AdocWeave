= AdocWeaveロードマップ

このロードマップは予定する能力をまとめたものであり、リリース日を約束するものではない。
正式なタスク定義はxref:../issues/README.adoc[Issue計画]に記載する。

== 実装済み基盤: Issue 001から026

実装済み基盤には次が含まれる。

* losslessなUTF-8ソース範囲、行索引、診断
* 段落、タイトル、セクション、constrainedな強調、斜体、等幅
* リテラルブロックと限定的なソースブロック
* HTML変換、Lint、保守的なFormatter、CLIコマンド
* Language Serverの診断、シンボル、アクション、Formatting、hover、補完
* 資源上限と、未対応構文に対する厳格・寛容ポリシー
* 出力形式に依存しないsemantic AST、Core 0.1規範文法、置換モデル
* Zed開発用拡張

Linux版Zedでの初期受け入れ確認は2026-07-20に完了した。LSP文書シンボルをOutlineへ
表示するには、ZedのAsciiDoc言語設定で`document_symbols`を`on`にする。

== 実装済み: 公開コア、レンダリング契約、参照構文: Issue 027から031

この段階では次を実装した。

* 構文プロファイルとキャンセル可能な公開処理API
* 明示的なHTMLレンダリングポリシーと出力契約
* nativeおよびWASMホスト向けのバックエンド非依存処理結果
* 汎用文書属性とホスト提供の保護属性
* 明示アンカー、安定した見出しID、共通参照先索引
* URLポリシー付き通常リンク
* 文書内・文書間・`note:`スキーム付きxrefの共通`Reference`モデル

== 実装済みの標準構文: Issue 029から034

この段階では次を追加した。

. ブロック継続を含む順序付き・順序なしリスト（完了）
. 標準cross referenceとノート参照を解決するホスト提供Resolver境界（完了）
. STEMおよびLaTeXノード（完了）

リンク・xref以外の一般的なバックスラッシュエスケープ、unconstrained書式、passthrough、
その他のAsciiDoc置換動作は
仕様上の未対応項目であり、この段階と合わせてIssue化する必要がある。より広いAsciiDoc
互換性を主張する前に規範文法へ追加しなければならない。

== 解析基盤の再設計: Issue 044

持続可能な機能追加に先立ち、次を破壊的に再設計する。

* 入力、CST、AST、行索引、診断および参照を所有する単一`Analysis`
* CLI、Formatter、HTML、LSPによる同一解析スナップショットの再利用
* 標準属性とxrefからノートアプリ固有の型・UUID規則を分離
* HTML契約version 2と一元化した処理上限
* property testと契約不変条件の前倒し

== AdocWeave内のホスト非依存統合: Issue 035から040

この段階では次を導入する。

* 汎用参照グラフと検索テキストのprojection（Issue 035完了）
* ホストが利用できる実行・キャッシュ・キャンセル境界（Issue 036完了）
* WASMとWeb Workerの統合（Issue 037完了）
* 定義移動、参照検索、文書リンクを含む残りのLanguage Serverプロトコル機能（Issue 038完了）

Language Serverの機能拡張に先立ち、Issue 045で手書きJSON-RPC基盤を
`async-lsp`へ移行し、`adocweave-lsp`の実行runtimeとしてTokioを採用する。
文書更新通知は受信順に適用し、読み取り要求は所有スナップショット上で有界に並行実行する。
`async-lsp`、TokioおよびLSP型はアダプター内に限定し、コア解析APIへ依存させない。
実行モデルと移行時の不変条件はxref:language-server.adoc[Language Server設計]に記載する。

コアはファイルシステム、ネットワーク、時刻、環境変数、UUID解決へ暗黙にアクセスしない。
ホストがこれらの能力を明示的に提供する。

ノートメタデータの型検証、DB投影、アプリ用サーバおよび製品統合は外部リポジトリが
所有する。

== 適合性とコア公開: Issue 039から041

最後の予定段階では次を追加する。

* fuzzing、property test、セキュリティ回帰テスト
* nativeとWASMで共有する適合性fixture（Issue 040完了）
* ホスト非依存コアプロファイルのリリース検証（Issue 041完了）

Issue 039では、解析、公開バイトAPIおよびHTML出力のfuzz target、決定的property suite、
セキュリティ回帰fixtureを導入した。運用方法はxref:testing.adoc[品質保証とFuzzing]に
記載する。Issue 040ではこの基盤をnative・WASM共通fixtureへ拡張する。

== Core Profile 1公開後の持続可能性改善: Issue 046

公開後レビューで、derived `Debug`に依存するAST適合性表現、runtime依存の`usize`処理上限、
分散した契約versionおよび過大なfuzz成功ログを改善対象とした。Issue 046では明示的な
canonical schema、固定幅上限、machine-readableなrelease manifestへ破壊的に移行する。

== ドキュメント保守規則

リポジトリのドキュメントはAsciiDoc（`.adoc`）で記述し、`.md`ファイルを受け入れない。
構文を変更するたびに次を更新する。

* xref:syntax-support.adoc[対応表]
* xref:compatibility.adoc[外部仕様との互換性評価]
* xref:grammar.adoc[規範文法]
* 該当する場合はxref:substitutions.adoc[置換モデル]
* 対応するfixtureとテスト

`docs-check`は`.md`ファイルの混入だけを検査する。将来のAsciiDoc内容検証は、
AdocWeave自身を使う別タスクとして追加する。

= Expected interpretation

- `word`-internal backticks and `some_identifier` remain plain text.
- The next line is one strong span containing emphasis and monospace children.
- The unclosed emphasis reports `unclosed-inline`; the later monospace span is
  still parsed.
- Literal-block inline markers and `<tag>` remain opaque. `.....` is content,
  not a closing delimiter.
- Source-block inline markers and `<tag>` remain opaque and the language is
  `rust`.
- The last literal block reports `unclosed-block` and recovers before
  `== Recovered heading`, which remains a heading node.
- The CST reconstructs the fixture byte-for-byte.
